# L2-b · Task 9 Steps 3-5 收官账(2026-08-06)

> 计划:`docs/superpowers/plans/2026-07-31-orchestration-L2b-data-runtime.md` · 执行:控制器本人。
> 前序:Steps 1-2 与部署点见 `2026-08-02-L2b-task9-deployment.md`。

## 会话与窗口

真机日期 **2026-08-06(周四)18:45,收盘后**。这是结算新鲜度成立的窗口:
venue 行情时间戳锚 = 08-06(本身是会话)→ 结算会话 = 紧邻其前的 **08-05**;
`available_at` = 08-05T07:00Z ≤ `as_of` 08-05T16:00Z(08-06 会话午夜),
且 08-05 正是 08-06 的紧邻前一会话 ⇒ 按 `max_trading_sessions=1` 判定 **fresh**。

## Step 2(本会话重跑)

`lane0_driver propose → approve → run`,2 次 LLM(settled_tokens=8192)。
ContextSnapshot `bootstrap-run-ctx-020368cc…`(payload-386,deep_lane_sees=True);
manifest `lane0-capture-2026-08-06`(content `cbd093289fbe…`,payload-385)。
输入 **9 present / 8 absent**(较 08-02 多 `today_tape`);
徽章诚实:`pit_inputs_partial` + `regime_missing` + `rotation_missing`
(本次 `lane0.rotation` 也 incomplete,较 08-02 多一项缺席 —— 模型侧,如实记)。

## Step 3 — 深链完整跑通 ✅

租约 `12e5649be24db3ef…`(1 准入 / 6 LLM / 2h,键结密封 reduced 记录 `2c4b4e0d`);
策略 `strat_mrgjipyt4s0` 临时 opt-in(跑完已还原,备份保留)。
**run `deep-afd5c0859c88718f`(300308,268 秒)**:

| 节点 | 终态 | 说明 |
|---|---|---|
| pv.price_action / pv.microstructure | FAILED `AuxDataUngranted` | Task 6 典型拒(L3 授权缺口),非 bridge_execution_error |
| sentiment / bull-r1 / bear-r1 / research-mgr | COMPLETED | |
| **pm** | **COMPLETED** | 含真数据读,见 Step 4 |
| **trader** | **COMPLETED** | ← 08-02 排练弹在此解码失败,本次通过 |

产物:`PortfolioDecision@1`(payload-446,rating=Hold)+ **`PortfolioTargetProposal@1`(payload-451)**
:300308 target_weight 0.5 / cash_weight 0.5 / confidence low。
台账 orchestrated 行**已落**(`var/seats_decisions.jsonl`,1 行):
`source='orchestrated'`,`run_id=deep-afd5c0859c88718f`,`cash_weight=0.5`,
badges = `subject_run_scoped_v1` + `reduced_evidence_preset_v1` + `reduced_evidence_missing:pv.technical,text.news`。

## Step 4 — 真读判据 ✅(一项未达成,如实记)

pm 的耐久 `NodeRun` 里:

- **1 条 `ToolCallRecord`**,tool = `cap.data.verified_snapshot`,结果 schema = `VerifiedSnapshotDataResult`;
- `DataRequest` 参数**逐字回显本 run 提交的 subject**:`{code: 300308, exchange: SZ, board: chinext}`、
  `as_of = 2026-08-05T16:00:00Z`、`mode=online`、`data_snapshot_id = lane0-capture-2026-08-06`;
- 结果 **`status=ok`**、`badges=['SETTLED_PRIOR_SESSION']`、
  **`pit_audit.guard_result=passed`**、`latest_available_at = 2026-08-05T07:00:00Z ≤ as_of`;
- 诚实警示原文点名结算会话 2026-08-05 与「两个价格槽是同一结算观测,0.00% 涨跌是构造使然」;
- **渲染块进了 pm 的 prompt**:`PromptAssemblyRecord.untrusted_blocks` 1 块,
  `trust=untrusted_data`、`method=verified_snapshot`、`rendered_length=1299`。

**未达成的半条:pm 行文没有引用这条读数。** 在 `PortfolioDecision` 全文里检索
`verified_snapshot` / `2026-08-05` / `settled` / `结算` / `收盘` 全部为 0;
pm 的论述写的是「唯一上游读数是 SentimentReport,证据基底为空」。
即:**数据真读到了、可审计、PIT 合格,但模型没把结算收盘价当作可引用的研判证据**。
这与 08-02 排练弹同形(那次 pm 也没引)。

**终审复审的更重判读(I-3,已采纳,修正本节原措辞)**:pm 不只是「省略未引」——
它在 `PortfolioDecision` 里**断言**「no single event or independently sourced datum」,
而当时一个 1299 字节、PIT 合格的数据块就在它的 prompt 里。原措辞把这件事记得太轻。
另需注意:`PromptAssemblyRecord` 证明的是**组装意图**,不是发出去的**线上字节**;
要判定归因(合理沉默 vs prompt 缺口 vs 块根本没上线),下一次实验**必须留存请求体**。
本任务不修,升级为待办 #1(带上述取证要求)。

## Step 5 — 账本(本文件)

本次真跑证明:**一条深链在生产上从 Lane-0 快照 → 主体投影 → 真数据世界 → 真 LLM 辩论 →
组合决策 → 目标提案 → 台账落行,全程走通且每一步可审计。**
未证明:pm 会引用数据块;完整十节点 preset(仍 L3);卖出/减仓在真持仓下的行为(台账无持仓)。

## 与 08-02 排练弹的差异(两次真跑对照)

| | 08-02 排练弹 | 08-06 收官 |
|---|---|---|
| trader | INCOMPLETE(解码 None) | **COMPLETED** |
| PortfolioTargetProposal | 无 | **有**(payload-451) |
| 台账 orchestrated 行 | 无 | **有** |
| 结算会话 | 2026-07-30 | 2026-08-05 |
| Lane-0 输入 | 8 present / 9 absent | 9 present / 8 absent |
| pm 引用读数 | 否 | 否(同形) |

## 出口闸清点(Task 9)

Step 1 部署 ✅(08-02,一列车点)· Step 2 真数字摘要快照 ✅(08-02 + 08-06)·
Step 3 深链完整 ✅ · Step 4 真读判据 ✅(pm 引文半条未达成,已记)· Step 5 账本 ✅(本文件)。

## 终审复审补记的两条(M-1 / M-2)

- **M-1**:Step 3 里「降级在 bull/bear 的 prompt 字节中陈述」这条,在 **reduced preset 下结构上不可满足**——
  pv 两个 aux 节点在该预案里没有任何下游消费者(复审自 preset JSON 与真 InputSnapshot 双证)。
  代码里对此是诚实标注的,但两份账本都没记,补记于此:该条判据只在密封十节点 preset 下有意义(归 L3)。
- **M-2**:pv 的 `reason_code` 没有生产读者,对外仍显形为通用 `handler_error`;
  典型身份靠 `error_type` + reason 首 token 承载(Task 6 复审已记,此处并档)。

## 待办(不藏)

1. pm 行文不引用数据块 —— 归因未定,需单独实验(改 prompt 要求引用 vs 判定其为合理沉默)。
2. 2027 日历材料年底前必须提交(现为响亮悬崖)。
3. `DataResult.warnings/badges` 落在渲染器可信框架内而类型系统不知情(封闭徽章词表,归 L3/终审)。
4. `G:\stocks` 的 `quote_time` 键两仓皆无钉(非 git 仓,静默回归风险)。
5. 完整十节点 preset 仍被支持检查拒绝 —— L3 补授权后才通电。
