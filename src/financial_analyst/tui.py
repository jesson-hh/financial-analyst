"""TUI module. Task 32 implements the Rich TUI + REPL. Task 31 ships stubs."""
from pathlib import Path


async def run_tui():
    print("[stub] TUI not yet implemented (Task 32)")


async def run_report_oneshot(code: str, asof, out_dir: Path):
    print(f"[stub] report {code} asof={asof} out={out_dir}")
