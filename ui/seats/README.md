# seats — 席位 · 落子(A4)

| 项 | 值 |
|----|----|
| 页面 | 观澜 · 落子.html |
| 入口组件 | `LuoziApp`(luozi-app.jsx) |
| 配套 jsx | luozi-chart / luozi-data / luozi-fleet / luozi-foundry / luozi-panels |
| 后端 | `/seats/daily`(guanlan 自有路由 `guanlan_v2/seats/api.py`,日线真K)+ `window.GL`(席位/卡,本地) |
| 闭环位置 | card + factor → **seat(装配)** → decision(落子) |

## 职责
**席位装配 + 落子**:把经验卡 + 因子在"校场(foundry)"里装配成**席位**(一个带信条 creed 的策略位,如反转席/动量席/事件驱动席/风控席),再"落子"=形成交易决策。

> **价格行为(价量几何 + 方法论 prompt,`?v=20260614g/i`,2026-06-14)**:借鉴 PA_Agent(AGPL)clean-room 重写,给落子每个策略实例装两层。①**确定性价量几何特征**:后端纯函数 `guanlan_v2/seats/price_action.py`(`compute_pa_features` 从 OHLC 算 实体/上下影/收盘位/振幅÷ATR/距EMA20/K线型态[趋势阳阴·内含·外包·十字]/突破前5/连续内含/量比/**涨跌停按板**[主板10·创业科创20·北交30·ST5]/跳空/跟随确认[PIT 向后看不取未来]+近3根),前端 `paFeatures()` 一套 JS 镜像(**契约**:同公式/枚举,改一边必同步;live 黄金用例与 Python 逐位一致),scanSeat 三模板加几何硬过滤+置信+诚实注释(替掉 MA/量比拍脑袋骨架,每决策带真 `geo`)。②**可编辑方法论 prompt**:校场每策略「价格行为」开关(**默认关·opt-in·存量不变**)+ 开启露出可编辑 textarea(预填 A股 做多读法默认模板)。decide 接线:**几何始终算**并随响应回 `pa_features`(几何常显);**仅 pa 开**才把「价量形态·确定性」块 +「价格行为读法」方法论注入 LLM prompt;落盘 `pa`/`pa_features`。决策卡新增「价量形态·确定性」块(徽章**确定性·非LLM**,源 `decide.pa_features||dec.geo`)。**2026-06-15(`?v=20260615a`)三条 decide 入口全接 pa**(此前只单笔「agent 研判」`runDecide` 接):复盘逐 bar「✦ 让 agent 真跑」`runRealThink`(luozi-app.jsx)+ 实盘定时研判 `runTimedDecide`(luozi-panels.jsx)均把策略 `pa`/`paMethod` 透传 `/seats/decide` —— **实盘与复盘的 agent 现在都吃到几何+方法论**(默认关不变,开关在校场)。**验真**:price_action 14 单测 + decide 接线 3 单测(pytest 362);浏览器 `?v=20260614g/i` 0 解析错、live `lzPaFeatures` 与 Python 黄金用例逐位一致(body0.75/close_pos0.917/趋势阳·主板涨停 vs 科创正常·突破前5高)、校场开关默认关→开后方法论预填(含 T+1)、**真 deepseek 跑立昂微 SH605358@06-11 pa=true 回 15 键真几何**(小阴·body0.223·振幅1.102×ATR·量比1.07·距EMA20 0.189)+ 落盘 pa:true(验后清测试记录)。计划/spec:`docs/superpowers/{plans,specs}/2026-06-14-luozi-price-action*`。**挂账**:几何升格引擎因子库一等公民(全平台复用)、几何进 P3 加权信号、PA_Agent 式多 setup 路由、事件模板 event 标志仍合成。

> **右栏交互重构(实盘清晰化 + 舰队每股信号 + 自选/盯盘,`?v=20260615b~e`,2026-06-15)**:① **右栏可滚动**:右栏容器(luozi-app.jsx)加 `overflowY:auto`+`overflowX:hidden`,末尾决策卡包裹改 `flexShrink:0/minHeight`,内容超高时栏内纵向滚动不再被裁。② **实盘精简掉 scanSeat「信号队列」**:删 `DecisionFlow`(它是 scanSeat 价量启发式扫描、非真信号、易误解),换成 `LiveDecideFlow`(只读 `realDecs[code]` 真 LLM 研判:定时/手动/哨兵/真跑,带「真·LLM」徽章 + 思维链展开,按开账日起算;诚实空态);台账标题加「组合账·跨股共享」徽章(实盘=一个组合,跨股一本账,后端 `/seats/ledger` 真持久 append-only)。③ **舰队每股信号列**:舰队视图从「只有网格、点卡跳单标」→「网格 + 右栏 `FleetSignalList`」,每行 = 名/代码 + scanSeat 最新信号(徽章「非LLM」)+ 今日真 LLM 研判(若有,徽章「真·LLM」)+ 盯/自选 开关;点行聚焦切单标。④ **自选 vs 盯盘**:`luozi-data.jsx` 加独立 localStorage 映射 `guanlan:lz:monitored:v1`(与池数组解耦,默认全自选·opt-in 盯盘)+ `lzPoolIsMonitored`/`lzSetMonitored`;盯盘=可自动研判+自动落账,自选=只看(手动研判/调仓仍可);`runTimedDecide`(自动研判)与条件单落账 `onTrigger` 各加 `lzPoolIsMonitored(code)` 闸门;舰队卡/信号列加盯/自选 chip(点→`lzSetMonitored`取反+`setPoolTick`重渲染)。⑤ **文案诚实化**:台账「9:30 起随行情」(无 9:30 时间锚误导)→「盘中自动研判→落账:仅研判循环开启+页面在线+盘中(有实时报价)时;无后端定时器,关页面即停」。**纯前端**(不动后端、无需重启 9999)。**红线**:scanSeat/启发式信号必带「非LLM」徽章、只有 realDecs 真研判才「真·LLM」,绝不冒充。spec/计划:`docs/superpowers/{specs/2026-06-15-luozi-right-panel-rework-design,plans/2026-06-15-luozi-right-panel-rework}.md`。

> **舰队多股 agent 盯盘(校场绑定驱动 · 页面驱动,`?v=20260615h~i`,2026-06-15)**:把上一期的「舰队 盯/自选 点击 toggle + monitored localStorage」退役,改成 **校场绑定为唯一真相**——在校场给某票绑了 agent(策略 `bind` 含该 code)= 盯盘;不绑(全局默认 bind=[])不算。① **数据层**(luozi-data.jsx):`monitoredCodes()`(所有显式 bind 非空策略的 code 并集)/`monitorAgentFor(code)`(第一个绑它的策略 = owning agent)/`poolIsMonitored(code)` 重写为绑定派生;删 `setMonitored`/localStorage、退役 `lzSetMonitored`、导出 `lzMonitoredCodes`/`lzMonitorAgentFor`。② **多股盯盘循环**(luozi-app.jsx,**页面驱动·不动后端**):`fleetWatch` 主开关(默认关·opt-in·不持久,防意外烧 LLM)+ `monQuotes` 盯盘票实时报价轮询(复用 `/seats/quote`,挂在 fleetWatch 上、与 mode 无关);盯盘循环 effect(deps 只 `[fleetWatch]`,**latest-ref** monQuotesRef/realDecsRef/recordRef 防 60s interval 陈旧闭包)逐股「真报价 `q.fresh` 判盘中 + per-code clock 节流(hourly/daily + 10min 地板,读 realDecs.ts 与单股循环共享去重)」真调 `/seats/decide`,成功 `recordLiveDecide` 写 `realDecs[code]`(带 ts)+ 已开账落 ledger decision;`onLiveDecide` 改为 recordLiveDecide 的焦点壳。③ **舰队右栏**(luozi-fleet.jsx FleetSignalList 重排):头部「盯盘·自选」+ 主开关「○ 开始盯盘 / ● 盯盘中·N 支」;盯盘行显 owning agent 名 + 真研判(realDecs 最新:方向/置信/时间/「真·LLM」徽章,无则「盯盘中·待研判」)+ 盘中点;自选行**只名字/代码 + ○ 自选,无信号**(删 scanSeat 速览)。FleetCard/FleetGrid chip 改只读徽章、去 onToggleMon。④ **落账闸门**:onTrigger 的 `lzPoolIsMonitored` 随重写自动变绑定派生(自选票不自动落账,语义不变)。**红线**:盯盘行只 realDecs 真研判 +「真·LLM」;自选行无信号;LLM 失败不写不落账;页面驱动「关页面即停·无后端定时器」明示。**验真**:`lzMonitoredCodes()` 由现有 bind 派生 `["300750","605117","688012"]`、默认策略不算;校场绑定→盯盘端到端(temp 绑 600519 → maotai 转盯盘 owning agent 对、删→复原,零污染);舰队 300750 显「● 盯盘·待研判」、其余「○ 自选」只名字、无「非LLM」;主开关 ○↔●·3 支;控制台 0 报错。盘中真触发待交易时段实跑。**挂账**:真后端 scheduler(无人值守)、多 owning agent 合议、盯盘研判历史留存。spec/计划:`docs/superpowers/{specs/2026-06-15-luozi-fleet-agent-watch-design,plans/2026-06-15-luozi-fleet-agent-watch}.md`。

> **30 分钟 agent 真跑(实验版,`?v=20260613j`,2026-06-13)**:复盘真跑支持 30 分钟级逐 bar 研判。后端 `/seats/decide` 加 `freq=30min`——date 当带时分 datetime、拉 5min 按 `_agg_5min_to_30min`(perGroup=6,与前端 frameData 同口径)聚 30 分钟序列、PIT 只取 ≤决策时刻的 bar、`compute_factors`(纯窗口运算)算「20 根 30 分钟 bar」窗口因子(rev_20 从「20日」变「20根30min bar」≈2.5 交易日)。语义 `render_factors(unit=)` 分钟标「根30分钟bar」防误读。前端 `lzBars30(symbol)` 全量聚合、`runRealThink` 放开 `tf==='30'`(迭代 bars30、跑最近 2 周末 10 交易日、传 datetime+freq、run 头 `tf:'30min'`)、`runPerf`/runDecs 按 `selRun.tf` 映射 bars30。**验真**:立昂微 30 分跑 5 次 2 周(05-29~06-11),每次 80 笔决策;**同日多根 30 分钟 bar 研判不趋同**(06-11 RSI 53.5→29.1→41.0、量比 2.89→0.59→1.35,10:00 开盘放量触发买入其余观望)=日内分辨率铁证;净值 +25.7%/Sharpe2.48 由 bars30 真决策模拟成交。**实验边界**:复用日线 compute_factors(完整分钟因子库=日内动量/开盘缺口/分时量能 另案);新闻/大盘不接;K线30分图金框 marks 对齐挂账(审查以流水+RunDecCard 为主)。计划 `docs/superpowers/plans/2026-06-13-minute-level-agent-backtest.md`,pytest 220 绿。

> **重排(`?v=20260613d`,2026-06-12):回测历史 run 化 + 单 agent + 实盘仓位台账**。
> ①**复盘以「回测历史」为第一公民**:每次「让 agent 真跑」=一个 run(前端生成 run_id 随每笔 `/seats/decide` 透传落盘,跑完 `POST /seats/runs` 注册头到 `var/seats_runs.jsonl`);右栏第一位 RunPicker 列本票 run(`GET /seats/runs?code=` 数字核匹配),选中 → 决策流水换源为该 run(`GET /seats/decisions?run_id=`)+ K 线金框标记跟随(scanSeat 骨架标记退场)+ 点行出 RunDecCard(rationale/证据/思维链;run 决策 shape 无 ev/price,不走旧 DecisionCard)。不同 run 绑不同策略=对比 agent 判断。**演武(确定性回放)不落盘、不入回测历史**。
> ②**单 agent**:多席合议退役——左栏=「当前策略」单卡(creed/clock/指标/切换列表),`active=[curStratId]` 保数组形状,OrderWatchPanel 受控 `seatId`,头部/净值线/舰队标签全换当前策略名;`lzConsensusEquity` 留存无活跃调用。
> ③**实盘=仓位台账(后端持久)**:`var/seats_ledger.jsonl` 事件流(open/trade/decision),`POST /seats/ledger` 校验现金/持仓,`GET /seats/ledger/state` 重放出 现金/持仓(avg_cost)/净值(日线 MTM,缺价诚实 null)/按日分组;LedgerPanel 开账(初始资金+起始日+导入影子持仓)→ 今日置顶+往日折叠;盘中研判/条件单成交自动落账(**第一期前端驱动,页面在线时;后端 scheduler 挂账二期**);MetricsStrip live 区=台账口径,影子组合 lzShadow* 降级过渡。哨兵轮询 `exclude_runs=1` 防 run 回放刷屏;置信校准剔除 run 记录防 PIT 污染。计划:`docs/superpowers/plans/2026-06-12-luozi-run-rework.md`。

> **第3期(`?v=20260608d30`)已把固定席位泛化为用户自命名的「策略实例」(StrategyInstance)**:在校场可**起名 + 选模板(动量/反转/事件)+ 配方(卡/因子/研报)+ 设时钟(止损/止盈/最长持有/执行TF/决策频率)+ 绑票**,存入 `window.GL`(`type:'strategy'`)。桌面**按当前票**显示其绑定策略,合议=它们等权组合;演武按 `template+clock` 跨绑票回测并回显平均持有期校验 maxHold;研判/影子按 `strategy.id` 记账。**诚实**:确定性落子/演武只用 template 进场 + clock 出场真算,配方因子仅作依据展示 + 喂 LLM,不冒充因子回测。首载 `seedDefaultStrategy` 种一个全局「动量·默认」承接旧单席位。设计/计划见 `docs/superpowers/specs|plans/2026-06-08-luozi-phase3-strategy-instances*`。

## jsx 分工(设计稿)
| 文件 | 职责 |
|------|------|
| luozi-app.jsx | 主应用 / 布局 |
| luozi-foundry.jsx | 校场——挑经验卡+因子装配成席位 |
| luozi-fleet.jsx | 席位群展示 |
| luozi-chart.jsx | 走势/图表 |
| luozi-data.jsx | 数据 |
| luozi-panels.jsx | 面板 |

## 数据
- **K 线 = 真数据(日线 + 历史 5min)**:`luozi-data.jsx` 经 `GET /seats/daily?code=&freq=day|5min&n=`(guanlan 自有后端 `guanlan_v2/seats/api.py`,读 stock_data 经引擎 `loader_factory.get_default_loader()` → `get_data_paths`,零硬编码)拉真 OHLCV → 装配进单标的复盘。无后端(`file://` / 后端未起)时**诚实回退合成样例**,顶栏标「样例」。
- **可变时间尺度 = 真数据**:TF 选择器 周/日/60/30/15/**5**/1 分全跑真数据 —— 日线(`freq=day`)直用、周线由真日线聚合;60/30/15 分由真历史 5min(`freq=5min`,日内 TF 时懒加载 ~2400 根)按日分组、按 12/6/3 根 5min 聚合;**5 分为原生(perGroup=1,真 5min 直用,近 3 日窗口)**、1 分由 `/seats/bars_live?freq=1min`(引擎 pytdx,实时,近 2 日)。真 5min 未到/窗口越界 → 回退 `subdivideDay` 合成。顶栏分别标「真·日线 / 真·5min / 真·1min / 日内·合成 / 样例」。
- **席位配方 = 真 GL 状态**:`GL.all('seat')` / `byRef`;seed 4 席位(各引一张卡 + 一个因子)。
- **其余证据 = mock**:每次落子的「触发·量化因子 / 引用·研报 / 命中·经验卡 / 当时·市场 regime」仍由 `luozi-data.jsx` 的 `evidenceFor()` 模板生成(见「开放项」)。

## 状态
**Slice 1(K 线接真)已完成并控制端独立验证**(2026-06-04):
- 后端 `/seats/daily` 经 TestClient + 真 HTTP + 浏览器三重核:`300750`→`SZ300750`、2026-06-02 真收 433.89;坏 code → `ok:false` 优雅降级。数据当前到 2026-06-02。
- 前端 `preview_eval`(9998 验证实例)核:`GUANLAN_BACKEND=同源`、`rootKids>0`、价格轴 224–483(真宁德,非合成 ~250)、顶栏「真·日线」、浏览器 `GET /seats/daily 200`、**无 console error**、jsx `?v=20260604s1` 生效。
- 席位策略(`scanSeat`)/指标(`metricsOf`)现在在**真日 K** 上跑出真决策标记与真收益曲线。
- 设计稿渲染(navTabs:5)沿用,UI 艺术风格未动。

**Slice 1.5(真 5min + 可变时间尺度)已完成并独立验证**(2026-06-04):
- 后端 `/seats/daily?freq=5min` 经 TestClient + 浏览器核:`300750` 真 5min,2026-05-29 15:00 收 424.0;历史 5min 自 **2018-01** 起(`600519` 97776 根)。
- 前端 `preview_eval`(9998)核:切「15 分」→ 顶栏「真·5min」、价格轴 405–432(真宁德日内)、浏览器 `GET /seats/daily?freq=5min&n=2400 → 200`、**无 console error**、jsx `?v=20260604s2` 生效。
- D/W 真日线(及聚合)、60/30/15 真 5min 聚合,失败回退合成并诚实标识。

**Slice 1.6(图表导航条:尺度缩放 + 时间轴平移)已完成并独立验证**(2026-06-04):
- K 线下方新增 `ChartNav`(luozi-app.jsx):尺度 `−/+`/滑杆调可见 K 数、滑动滑杆沿时间轴平移、「全」复位;读出「起–止 · 可见/总 根」。切 TF/标的/模式自动复位为适配全部。
- 修复:导航条改动暴露了 EquityChart/CandleChart 在「合成→真」swap 瞬态的越界(`eq[rt]`/`bars[i]`)→ 每次加载刷一串 `NaN cy` / CandleChart 抛错。已在 `luozi-chart.jsx` 加防御钳制(视窗 clamp + 圆点 `isFinite` 守卫),末态本就自愈,现瞬态也不再报错。
- 验证(`preview_eval` 9998,全新缓冲):缩放 250→60、平移到中段窗口、切 15 分=真·5min,**全程 `No console logs`**;jsx `?v=20260604s4`。

**Slice 2(证据链逐层接真,上游就绪后)进行中**(2026-06-05,jsx `?v=…b1`,均 Playwright 验证 0 error):
- **① 市况条 regime + 涨停/跌停**:`MarketBar` 接 `/watch/market_status` 真今日值(震荡 / 157 / 80),顶栏「真·今日」;主线 `mainline.top` 上游仍空 → 回退 mock。`lzFetchMarketStatus`(luozi-data)。
- **#7 决策卡研报(A+闭环)**:研报位 = 本股深度研报存在性(`/report-progress?code=` → 16/16 agents·asof,「看全文」跳对话模块)+ 席位经 GL `seat→card→research` 引用的真 research 素材(§);去掉假 `ev.research`。`lzFetchReportStatus`/`lzSeatResearch`。报告全文读取归对话·研报模块。
- **② 校场料抽屉**:`lzSyncArchive` 启动把 `/cards/list`(12 真卡)+`/factorlib/list`(19 真因子)**幂等 merge 进 GL**(只增不改、跳过已存在 id);Foundry 组件**零改动**,读 GL.all 即显示真料(现 18 卡 / 23 因子)。
- **③a 决策卡实盘今日因子**:`mode==='live'` 时「触发·量化因子」按 code 从 `/watch/signal_pack` 取真今日 combo/FM/LGB(SZ300750 实测 47/14/#2691),顶栏「真·今日」,空字段(FM簇/v4 上游空)显 `—`。`lzFetchSignalRow`(整包缓存一次,按 prefixCode 查)。
- **③b 复盘真因子 · base 已上线并 Playwright 验证(2026-06-05)**:决策卡 **backtest** 分支「触发·量化因子」按 `(code, dec.date)` 取**真历史因子**。
  - 后端新增 `GET /seats/factors?code=&date=`(`guanlan_v2/seats/api.py`):**价量/表达式因子 34 项**(`financial_analyst.factors.core.compute_factors`)对任意历史日 **PIT 真算** —— 服务端 `end=date` 取 ≤D **含当日收盘**日线(qlib `_slice` 闭区间),复刻引擎 `FactorComputer._execute` 的 **EOD-of-D** 口径;**绝不**走 ≤T-1 的 `fetch_quote_leq_prev`(那是盘前决策链,另一条)。**模型因子**(fm/combo/lgb/v4)旁路 `load_signal_pack(date=D)`(**显式传 date**、绝不落到最新):写入侧只产单日(实测仅 2026-06-01)→ 历史 D `model.available=False`+null+note,**绝不**用今日值冒充历史 D。坏码/无数据/异常→`ok:False`+reason 恒 HTTP200。
  - 前端 `lzFetchSeatFactors(code,date)`(luozi-data.jsx,`(code@date)` 缓存);DecisionCard backtest 显真价量 5 项(反转20/动量60/RSI14/乖离20/量比)+「**真·MM-DD**」徽章 + 「价量已真算、模型历史待回填」诚实小字;live 不回归(仍走 `lzFetchSignalRow`)。布局逐字保留(仅 cells/标签换真)。jsx `?v=20260605b1`。
  - 实测(9998):`300750@2026-05-28` factors 34/34 真值(rev_20=0.044…)、`@2026-06-01` model.available 真(combo47/fm14/lgb#2691,与 ③a 一致)、`@2026-03-10` rev_20=-0.078(异日异值→**真 PIT、非缓存**)、坏码 ok:False/HTTP200;浏览器 backtest 点「卖出·动量席·2026-05-14」→「真·05-14 反转20+0.0%…」**0 console error**。

**Slice 2 待办**:
- **③b·phase2 FM/combo 历史真值 · PoC 已打通并 Playwright 可见(2026-06-06)**:复盘决策卡可显**历史真 FM/combo**(离线批算 → 缓存 → 旁路读 → 决策卡)。
  - **离线批算工具** `guanlan_v2/seats/fm_backfill.py`(只在 conda `stocks` 环境手动跑,带 GPU:torch 2.11 cu128/CUDA True;引擎 `G:/financial-analyst/.venv` 是 CPU-only、无关)。复用 `src.model.flow.predict` 内部件,**单次 model.sample 同出 fm+combo**(省一半);跨 6 簇(c1-c6,W11 ckpt train ≤2026-04-15)拼接 → **当日全市场横截面 rank** → `fm_pct`/`combo_pct`/`fm_cluster`,写 `var/seats_fm_backfill.parquet`(guanlan 自有、**不碰 G:/stocks**;按 date+code 去重)。跑法:`D:/app/miniconda/envs/stocks/python.exe guanlan_v2/seats/fm_backfill.py <日期...> --m 256`。
  - **后端**:`/seats/factors` model 旁路加**第二源** —— daily_signal_pack 未命中(历史 D)→ 读该缓存(`api.py`,只读 parquet);命中且非 NaN → `model.available=True` + `fm_pct/combo_pct/fm_cluster` + `lookahead`(`D≤2026-04-15`=W11 训练见过 ⚠);NaN/缺 → 诚实 null。
  - **前端**:`lzFetchSeatFactors` 带回 `modelAvailable/lookahead`;DecisionCard backtest 命中 → 显 `combo分位/FM分位/FM簇` + 2 项价量,徽章 `真·MM-DD`(look-ahead 加 ⚠),诚实小字注 OOS/look-ahead;未命中 → 退价量 5 项 + 「模型历史待回填」。逐字保留布局。jsx `?v=20260605b2`。
  - **成本**(实测):**单日 ≈ 11-12 min**,且 **dataset-build-bound**(m=64 674s ≈ m=256 723s)→ 用 m=256 满质量(近免费)。分位需全市场 → 每日一次 `predict_fm_all`(非每股)。
  - **实测可见**(9998):缓存 3 日(03-10/05-14/03-11);决策卡 `2026-05-14`(动量席·300750,**OOS>04-15**)显 **combo74/FM41/FM簇c5** + 「W11 OOS·PIT 重算」、0 error;`2026-03-11`(300750 该日 NaN)诚实退价量。**坑记**:研究侧 ClusterFMDataset 多进程 → 批脚本必须 `__main__` 守卫,**别** `python -`(stdin)跑(子进程 re-run `<stdin>` OSError)。
  - **遗留**:① 要点亮更多决策卡 = 把那些决策日(共 32 个落子,2025-06~2026-05)逐日 backfill(每日 ~12min;部分 megacap 如 300750 个别日 NaN,属正常、与 signal_pack ~36% null 一致,诚实退价量);② LGB/v4 历史仍占位(需重构 `v4_ranking.main()→rank_universe(date)`)。
- **✅ 决策卡「当时·市场状态」**(2026-06-06,jsx `?v=20260605b3`,Playwright 验 0 error):实盘显今日真 regime(`/watch/market_status`,实测「震荡」)+「真·今日」标;复盘历史 regime 无 PIT 源 → 留 mock + 诚实小字「当时 regime 历史无 PIT 源·示例值」。`lzFetchMarketStatus` 已有,DecisionCard 加 market state(仅 live fetch)。(要真历史 regime 须归档每日快照或对 ≤D 重算 dfm。)
- **✅ 决策卡「命中·经验卡」接 GL 真卡**(2026-06-06,jsx `?v=20260605b4`,验 0 error):`lzSeatCard(seat)` 取席位 `seat.refs` 第一张 card(GL 共享档案,与校场/验证区同源)→ 决策卡显 ◈卡名 + 真 insight + 验证/conf/IC;无则回退 `ev.card` mock。seed 卡 `real:false`(非后端 /cards/list)→ 不挂「真」标、注「GL 知识库」;经 syncArchive 的后端真卡才挂「真」。**决策卡证据链 4 层现已全部真/诚实**:触发·量化因子(③a/③b 真)· 引用·研报(真)· 命中·经验卡(GL 真卡)· 当时·市场状态(实盘真/复盘诚实)。
- **✅ ⑤ 席位真决策 agent + 真思维链(on-demand,2026-06-06,jsx `?v=20260605b6`,Chrome 实测 0 error)**:决策卡新增「席位·agent 研判(真)」字段 —— 点按钮 → `POST /seats/decide`(`guanlan_v2/seats/api.py`)**真调 LLM**(**deepseek-reasoner R1**,经 `LLMClient.for_agent('watch-agent').with_overrides(deepseek/deepseek-reasoner)`;默认 qwen 的 `DASHSCOPE_API_KEY` 实测 401 失效,改走 deepseek;`DEEPSEEK_API_KEY` 有效 + Clash 7890 代理)综合 量化因子(服务端 `compute_factors` PIT 真算,end=date,因子带中文标注去歧义防 R1 误读)+ 经验卡 + 研报 + regime,**逐步推理**后给 `{direction,confidence,rationale,key_evidence}` + **真思维链 `reasoning`**(R1 的 `reasoning_content`,~1-2k 字,决策卡「思维链 ▾」可展开)。reasoner 不支持 `response_format`,故 prompt 让它末尾给一行 JSON 再 regex 抽。代价:~17-20s/次(比 chat 慢,但有真 CoT)。**区别于** K 线 `scanSeat` 价量启发式标记(回放骨架/无 LLM):这是真模型秒级推理。实测 2026-05-14 动量席 → **观望/78**(扣住 mom60=0.177、RSI14=42.9、量比0.86、regime 高位派发),与启发式「卖出」不同 → 证明独立真思考。前端 `lzSeatDecide`(luozi-data)+ DecisionCard 真 spinner(deepseek 真调用、需几秒)。
  - **✅ 浏览器缓存坑已根治(2026-06-07)**:`guanlan_v2/server.py` 加 `_UIStatic(StaticFiles)` 子类,`get_response` 对 `text/html` 回 `Cache-Control: no-cache, must-revalidate`(**只 .html**;jsx/css 仍默认缓存,其 `?v=` 已足够 bust;**故意不用全局 middleware**——`BaseHTTPMiddleware` 会缓冲引擎 SSE 流)。现在 bump `?v=` 后**普通刷新(F5)即生效**,不再需要 Ctrl+Shift+R / `?cb=`。实测:HEAD `:9998` 该页 → `Cache-Control: no-cache, must-revalidate`;真机 Chrome 普通 F5 仍显新 UI(模式 toggle 在)。**史**:浏览器曾缓存 HTML 本身,只 bump jsx `?v=` 不够(script 标签还是旧版),必须硬刷新——此坑现已修复。
- **✅ ⑤ 研判「快/深双模式」toggle(2026-06-07,jsx `?v=20260607c1`,Chrome 实测两模式 0 error)**:决策卡「席位·agent 研判」加「模式」开关 —— **快·chat**(`deepseek-chat` + `response_format=json_object`,~几秒,无思维链)/ **深·reasoner**(`deepseek-reasoner`,~十几秒,带真思维链)。前端 `agentMode` 状态(默认 deep,跨卡保持用户偏好,不在 useEffect 重置);`POST /seats/decide` 收 `mode`(默认 deep 不回退):fast 走 chat+JSON object、deep 走 reasoner+CoT;prompt 末段、spinner、页脚均按模式诚实文案(快模式注「无思维链,切深看逐步推理」)。实测 2026-05-14 动量席·300750:快 → **观望/75**(真·deepseek-chat,无 CoT,~8s)、深 → **观望/90**(真·deepseek-reasoner,思维链可展开,扣住乖离-2.9%/量比0.86/高位派发,~20s)。
- **✅ ④ 实盘真盯盘 · 实时盘口接入(2026-06-07,jsx `?v=20260607c2`,Chrome 实测 0 error)**:实盘模式市况条(`MarketBar`)右侧「实时盯盘 · {日期}」mock 换成**真盘口** —— 新后端 `GET /seats/quote?code=`(`guanlan_v2/seats/api.py`,引擎 `/quotes` 同源 `TencentQuoteCollector` 腾讯实时)归一返 `{price,prevClose,open,high,low,changePercent,volume,amount,turnover_rate,vol_ratio,asof,lastBarDate,lastClose,fresh}`;`fresh = 报价日 > 最后真日 K 日`(盘中 True→「实时盘中」绿点跳动;盘后 False→「最新收盘」灰点静态)。前端 `lzFetchQuote`(luozi-data)+ luozi-app **live 模式轮询 ~6s**(切走 / 非 live 清空,失败保留上次不伪造)→ MarketBar 显 现价 / 涨跌%(红绿)/ 高 / 低 / 量比 / 换手 +「真·盘口」标。引擎 `TencentQuoteCollector._parse` 补 `asof`(f[30] 盘口时间戳,向后兼容,不破坏既有调用)。实测 300750:`最新收盘 · 06-05 16:14 · 403 · -1.27% · 高412.65 低402.7 量比0.69 换手0.68%`(今为休市→腾讯回最后收盘快照,price==最后日 K close==403、报价日==最后日 K 日→fresh=False、诚实标「最新收盘」;开盘时段自动转「实时盘中」并每 6s 跳动)。**遗留**:③ 实时价目前只入市况条盘口,未喂 scanSeat / 合议(避免在未完成 bar 上伪造决策)—— 用户暂缓。(①② 已于 c4 完成,见下条。)
- **✅ ④-①② 今日 forming 日K + 实盘日内拼今日 5min(2026-06-07,jsx `?v=20260607c4`,Chrome 实测 0 error;经 ultracode 勘探工作流 + 4 路对抗审查工作流 PASS)**:**①** live + 日线 + 盘中(`quote.fresh` 且报价日≠末日K日)→ 显示帧 `dispFrame`(=`frame.fbars.concat([forming])`,**绝不入 symbol.bars/scan/合议**)右端加一根**空心虚线「今」蜡烛**(O/H/L/现价 来自 `/seats/quote`);`luozi-chart` 按 `b.forming` 分支(空心虚线 + 今,全亮不淡化);as-of 墙在 live 今日端自动隐藏(`liveTodayEdge`,避免标「末日K日」矛盾)。**②** live + 日内 TF → `lzFetchRealtimeBars5`(引擎 `/watch/bars` 实时 5min)30s 轮询、去重拼到历史 `bars5` 末尾;`frameData(…, liveEdge)` 把日内窗口右端延伸到今日 → 今日 5min 聚合进 60/30/15 图。**红线守住**:forming/今日数据只入显示层,`symbol.bars/decisions/perSeat/bench` + `n` + 回测全不受影响(审查确认 `concat` 不改原数组、`bars5` 无 scan 消费者、无落子落在 forming/今日 bar);休市时 `fresh=False` + 今日 5min 去重 → 无伪造(实测两者皆诚实不显)。**已验**:模拟盘中报价(06-08)→ 今蜡烛 + 「实盘盘中」盘口出现;模拟今日 5min → 15分图右端拼出今日 bar(86 根含今日,窗口延伸到 06-08);复盘字节不变、0 console error。**审查修**:实盘 5min 轮询 effect 去 `realSyms` 依赖(防每次拼接重建 30s 定时器 → 请求风暴)+ forming bar 数值强制 `+` 转换 & `h/l` 夹 `o/c`(防异源字符串/过期 high 截顶)。
- **✅ 条件单 + 触发引擎(阶段2,2026-06-07,jsx `?v=20260607c6`,真 5min 回放 + Chrome 实测 0 error)**:"agent 出条件单 → 盯盘触发 → 落子"的核心,**一套引擎 live/回测共用**(回测=金标准验证,不靠开盘)。
  - **后端**:`GET /seats/live_eval?code=`(触发上下文:实时价 + MA5/20 + RSI + 量比 + 乖离,真值;3 股自洽实测)。`GET /seats/order?code=&seat=`(LLM `deepseek-chat` 按席位信条 + 真 ctx 出条件单 `{side,triggers[{kind,op,value}],logic,stop,take,note,validity}`,kind 限 price/volRatio/maDiff20/rsi14、服务端校验清洗)。引擎 `TencentQuoteCollector._parse` 已补 `asof`(见 ④)。
  - **前端引擎(luozi-data,纯函数)**:`buildTriggerCtx`(任意 bar 序列第 i 根算 price/high/low/ma5/ma20/maDiff20/rsi14/volRatio,PIT≤i)+ `evalTrigger`(单 bar 判定:price 走 **intrabar 触碰**——`>=`看 high、`<=`看 low,成交=触发价;余比 ctx 标量)+ `runTriggerReplay`(逐 bar 跑、首触即 fire)。`rsiOf`/`lzSeatOrder` 一并导出。
  - **UI(luozi-panels `OrderWatchPanel`,右侧栏·**非全屏**,挂在 DecisionFlow 与 DecisionCard 之间)**:选席位 →「⚡ 生成条件单」(真 /seats/order)→ 卡显 方向 + 触发 chips + 止损止盈 + 理由 →「▶ 检验触发(真 5min 回放)」→ ⚡触发卡(日期/成交价/该 bar 高低量比乖离)或诚实"未触发"。
  - **验真**(全真历史 5min、不靠开盘):①自查 harness——播种 4 单(到价/到价+量比/跌破/站上MA20)跑真 5min,引擎触发 idx **逐一等于独立另算的期望**(`allPASS`),AND 逻辑对(价+量都满足才触)、intrabar 成交价对。②UI 端到端——动量席 agent 出"价≥412.65 且 量比≥1.5 且 站上MA20"→ 真 5min 回放 **触发 @2026-06-02 09:35 成交412.65**(该 bar 高429.19/量比3.989/乖离0.0164 三条全满足)。
  - **✅ 2b-2b 触发标 K 线 + 标记视觉改(c7,Chrome 实测 0 error)**:OrderWatchPanel `onTrigger` 上报 → app `orderTriggers` → `CandleChart` 在到价那根 bar 标**发光金晕 B/S**(`barIdxForAt`:日线按日匹配、日内按时匹配)。顺带把全部买卖标记由 ▲▼ 改为**不靠颜色的 B / S**(墨字 `var(--ink-1)` + 白描边 `paintOrder=stroke`,压蜡烛也清晰、色盲友好),**预警菱形 ◆ 发光**(`<defs>` 的 `lz-glow` 高斯模糊滤镜);glow = 触发/预警 的统一视觉语言。底部图例同步改「B 买 · S 卖 · ◆ 险」。实测:动量席条件单触发 @06-01 14:25 → 06-01 日 bar 现金晕 B。
  - **✅ 条件单卡设计(c8,frontend-design skill)**:OrderWatchPanel 从「素 chip」重做成**朱砂令·水墨册页**(贴 `tokens.css` 中式克制语言,**非全屏、侧栏内**):印章红「令」+ serif 标题 + 印章红「⚡立单」pill;触发条件改为**条款式**(若/且 连词 + 指标名左·数值右,虚线分隔,不再挤 chip);守(止损·黛)/标(止盈·朱)/效期/真-model 印 一行;批语金线引;**触发结果 = 朱砂「触发」竖印**(paper 字)+ 成交价 + bar 明细,`lzSeal` 钤印动画 + `lzGlow` 柔光脉冲(html `<style>` 新增 2 keyframe)。0 console error。
  - **✅ K 线 B/S 标记设计(c9)**:从「墨字描边」改成**仿交易软件徽章** —— 朱砂(B·买,蜡烛下方)/黛绿(S·卖,上方)圆角徽章 + 宣纸白字 + 秀气字重(600 sans)+ `lz-stamp`(feDropShadow)钤印阴影;选中描环。**条件单触发徽章**金晕从「大金盘」收成**细金环 + 柔光**(`lz-glow`),与普通落子区分("发讯")。真 5min 回放触发 @06-01 → K 线现金环 B,实测 0 error。
  - **✅ ②搬实盘 · 盘中实时盯盘自动触发(2026-06-08 开盘实测,jsx `?v=20260608d1`,0 console error)**:OrderWatchPanel 收 `mode`;live 模式对已生成条件单**每 8s** 拉 `lzFetchLiveEval`(`/seats/live_eval` 真实时:现价+高低+MA/RSI/量比)→ **同一 `evalTrigger`** 比对 → 价到/指标满足即**自动触发**(无需点检验),弹朱砂「触发」印 +「照讯手机自落单·系统不代下单」+ `onTrigger` 标 K 线金环 B;未满足显「实时盯盘中…现价 X」;`fresh=false`(休市)只显示不判。**真盘验通**:300750 动量席 agent 出「买入 价≥402.68 且 量比≥1.5」→ 盘中(10:05)今日高触 402.68、量比1.54 → **实时触发 价402.68**,forming「今」candle 上现金环 B。引擎 live/回测完全同一套(ctx 源不同而已)。
  - **✅ 实时 1min 盯盘(2026-06-08 开盘实测,jsx `?v=20260608d3`,0 console error)**:K 线加「1分」TF。**后端**:引擎 `pytdx_kline.fetch_1min`(`KLINE_TYPE_1MIN`=8)+ `WatchFeed.bars1` + 新路由 `GET /seats/bars_live?freq=1min|5min`(transient WatchFeed,vol÷100 转手)。**前端**:`lzFetchRealtimeBars1` → live+1分 时 20s 轮询整批替换 `symbol.bars1`;`frameData` 加 tf='1'(源 `bars1`、perGroup=1 直用、winDays 2、liveEdge 延窗);`TfPicker` 加「1分」;scaleLabel「真·1min」。**1min 历史库个股为空 → 只走实时口**(回测 1min 无数据 → subdivideDay 合成兜底;已加 `endDay` 钳制 + `bars[day]` 守卫,修了切 TF 瞬态 `subdivideDay(undefined)` 的 crash)。真盘验通:300750 切「1分」→ 真·1min、60/480 根、范围到当前分钟、20s 刷新;15/30/60 分回归正常(真·5min)。
  - **✅ 5min 交易单(timeframe-aware order,2026-06-08 开盘实测,jsx `?v=20260608d4`,0 console error)**:`/seats/order` 与 `/seats/live_eval` 加 `tf=day|5min` 参数。新增模块级 `_ctx5_sync(c,q)`:实时 5min(`WatchFeed.bars5`)+ quote 算 MA5/MA20/乖离/RSI/量比/近端高低,**简单口径与前端 `buildTriggerCtx` 对齐** → 生成/live监控/回测检验**同一套**(彻底解了之前的 parity 注脚)。tf=5min 时 LLM 用「5min 日内短线」语气出更紧的单。前端:`lzSeatOrder(code,seat,tf)`/`lzFetchLiveEval(code,tf)` 带 tf;OrderWatchPanel 加「周期 日线/5min」下拉(`otf`)、卡上「5min 单」标、live 监控用 `order.tf` 调 `/seats/live_eval?tf=`。**真盘验通**:300750 动量席 5min 单 → 买入 价≥398.5 且 量比≥1.5、守395.5 标402(比日线单 412.65/400/450 更贴日内)、批语引「站上 5min MA20(397.98)」(非日线 419);实盘下「实时盯盘中…每8s 比对·现价」运行。
  - **✅ 原生 5min 尺度(2026-06-08 开盘实测,jsx `?v=20260608d5`,0 console error)**:用户「我没看到 5min 尺度选项」——此前 TfPicker 只有 60/30/15(由真 5min 聚合)与 1 分(实时),**缺原生 5 分**。补:`TfPicker` opts 插 `['5','5分']`;`frameData` 加 tf='5'(源 `bars5`、perGroup=1 原生直用、winDays 3、回退 k=48)+ lab '5分';`luozi-app` `intraday` 含 '5'、两处 `bars5` gate(懒加载 line 98 + live 拼接 line 117)含 '5'。**双模验通**:复盘 5min = 真·5min、144/144 根(06-03~06-05 三日×48)、MA/量/净值齐;实盘 5min = 真·5min、右端拼今日盘中(06-08 11:20、166 根、量比/价随盘动)、`实时盯盘中` 运行;两模 0 console error。
  - **✅ 顶栏标的行情条放大(2026-06-08,jsx `?v=20260608d8`,0 console error)**:用户「上面的框 再加一个当前股票的实时信息,股票实时信息太小了」——此前实时盘口挤在 `MarketBar` 右角(价 fontSize 13)。新增 `StockHero`(luozi-panels):嵌入指标条(`MetricsStrip` 左锚,放在「合议」标签前),**现价 30px**(朱涨/黛跌)+ ▲▼涨跌幅/涨跌额 + 开/高/低(高朱低黛)+ live 量比/换手/额、复盘 量;`MetricsStrip` 收 `symbol/rt/mode/quote`。**双模**:live=真实时盘口(/seats/quote)、复盘=当前游标 bar OHLC(随逐 bar 推演刷新)、实盘待报价=「等待报价…」不误标复盘。`MarketBar` 同步**删去重复小盘口**,只留市况宏观 + 右角「●实时盘中·时间」会话灯。**修一处数据诚实 bug**:腾讯 `amount`=f[37] 单位**万元**,原 `big()` 当元格式化 → 误显「62万」(实为 62.31亿);改 `×1e4` 转元 → 正确显「62.31亿」。验真:复盘 403.00/▼-1.27%/当前 bar;实盘 395.28/▼-1.92%/额62.31亿/真·盘口,0 error。
  - **✅ 市况行 主线去 mock(2026-06-08,jsx `?v=20260608d9`,0 console error)**:用户「这个主线是 mock 吧,不要随意写」——属实,且是**两层 bug**:① `fetchMarketStatus`(luozi-data)读 `t.name`,但 `/watch/market_status` 的 `mainline.top[]` 字段是 **`t.industry`**(无 name)→ 解析恒空 → mainline 恒 null;② `MarketBar` 主线 `|| '锂电 · 储能 · 光伏'` **硬编兜底** → 永远显这条假题材。修:① 读 `t.industry`(回退 name);② 去硬编、null→`—`。**顺手**:涨停/跌停 `38+rt%27`/`6+rt%11` 合成兜底、行情 `lzRegimeAt` 启发式兜底(注释自承 mock)同样去除 → 无真 `/watch/market_status` 时整行诚实 `—`(不再无标签编造)。**验真**:市况行真显 `行情 震荡 · 涨停 157 · 跌停 80`(全取自 /watch/market_status 今日 06-02 快照)。**后续(用户决定,`?v=20260608d10`)主线整条移除**:查到主线 `mainline.as_of=2026-05-26`(月级雷达,远早于快照日 06-02、更早于今日 06-08)→ 把"两周前的月级主线"挂"今日实盘"名下属 vintage 不符;用户「直接不要显示主线了」→ `MarketBar` 删主线 cell(`fetchMarketStatus` 的 mainline 解析保留,`DecisionCard` 仍用)。**注**:`DecisionCard`(落子卡·当时市场状态)也有一条主线(live=market.mainline 同源 stale、backtest=ev.mainline 带"示例值"标),用户若要一并去掉再删。
  - **✅ 市况行 实盘日期修正 + K线悬停浮窗贴光标(2026-06-08,jsx `?v=20260608d12`,0 console error)**:① 用户「实盘的时候这些日期显示也是错的」——实盘下 `日期` 原显 `b.date`(历史末根日 06-05,非今日),徽章写「真·今日」实为 06-02 快照。修:`MarketBar` 加 `rowDate`=实盘取 `quote.asofDate`(今日真交易日 06-08)、复盘取 `b.date`;徽章「真·今日」→「**真·快照**」(行情/涨跌停=/watch/market_status 最新快照,可能滞后今日,tooltip 注明)。验真:实盘行 `真·快照06-02 · 日期2026-06-08 · 行情震荡 · 涨停157 · 跌停80`。② 用户「鼠标放 K 线上,具体数据要显示在 K 线旁边而不是左上角和右上角」——`CandleChart` 悬停浮窗原钉死在左/右上角;改:`hover` state 存 `{idx,my}`,浮窗**贴在被悬停 K 线旁**(光标右侧·近右边界翻左)、纵向跟随光标 + 加横向十字光标线;`whiteSpace:nowrap` 一行一对(开收/高低/量)。验真:中段悬停浮窗紧贴蜡烛、近右缘自动翻左、OHLC 在高低区间内、0 error。
  - **✅ 研判循环 · 三触发(2026-06-08,jsx `?v=20260608d15`,0 console error)**:实盘让席位按节拍自动"会动"——`OrderWatchPanel` 把手动 `genOrder` 收进 `runJudge(reason)`(记「研判流水」),三触发穿插:① 手动「立单」;② 成交后(`liveFired` 实时触发 → 1.5s → 自动再判 → 新单重新盯盘);③ 定时(`loopOn`+live+`fresh` 盘中,每分钟查、距上次 ≥1h 封顶发起 `runJudge('timer')`;`fresh` 由 app `quote.fresh` 经新 prop 传入;冷启动 `lastJudgeRef=0` 使开循环后约 60s 引出第一单)。仅 live 显「○ 研判循环 / ● 循环中」开关,复盘无。复用 `lzSeatOrder`/`/seats/order`,**无后端改动**。**验真**:手动→「手动研判 买入」;真盘开循环 ~60s 后自动「定时研判 观望」+ 生成单(价≥410 且 量比≥1.5 且 站上MA20)+「实时盯盘中…每8s」;复盘无开关、立单/验触发正常;两端 0 error。**注**:`fresh` 在 15:00 收盘后、日K未纳今日前仍为真 → 定时可能略过点(沿用既有 fresh 语义);**位置感知研判(继续持/平)与影子组合(实盘绩效 B1)归第 2 期**。
  - **✅ 第2期·补 持仓感知研判 + 研判平仓(2026-06-08,jsx `?v=20260608d23`,后端 api.py)**:把"最小 B1"补成"完整 B1"的持仓管理半边。**后端** `/seats/order` 加可选 `hold_entry/hold_since/hold_days`(Query):有持仓→换"管理持仓"提示(继续持有=side观望 / 了结=side卖出,同 JSON 结构)+ 顶层加 `held/pnlPct`;无持仓行为逐字不变(回归)。**前端**:`seatOrder(code,seat,tf,hold)` 带 hold(**仅在有值时附参——空 `hold_days=` 会让后端 int 解析 422**);`shadowClose(shadow,posId,price,…)` 纯函数按现价平(`研判平`,null 价不平);`luozi-app` 把 `shadow.positions` + `onClosePosition` 传下;`OrderWatchPanel` 找本席位未平仓 `myHold`→显「持仓中·进场X·浮动Y%」+研判带 hold,研判判卖出且有持仓→按现价 `onClosePosition`(系统只发"该平"信号,用户手机自行操作);复盘不动。**验真**:后端 curl held:true/pnlPct 31.01,side 随判∈{观望,卖出}(持有+31%,LLM 真据量价判);`shadowClose` JS 验平仓/`研判平`/null守卫;实盘注入持仓→面板显「持仓中·进场300」;**修两 bug**:`shadowClose` 的 `+null=0` 绕过 isFinite(同 d20 改 `price==null||`)、`seatOrder` 空 `hold_days=` 致 422(改仅有值附参,curl 验 HTTP200 held:true)。**注**:整链视觉 e2e(立单→卖出→平仓渲染)因浏览器扩展掉线 + 当次 LLM 判观望未截图,各环节已分别验真。**剩(后续)**:`hold_days` 前端算真持有天(现传 null→提示"None 日");maxHold/跨票聚合/B2 回填真实成交。
  - **✅ 第2期 B1 影子组合(最小诚实版,2026-06-08,jsx `?v=20260608d19`,0 console error)**:把实盘"假合议(复用历史回测)"换成**诚实影子组合**,彻底消灭"实盘=复盘"名实不符。**当前票口径**(与按票合议对齐;跨票聚合留后续)。纯函数内核 `luozi-data.jsx`(`lzShadowLoad/Save/AddEntry/CheckExits/Metrics`,localStorage 键 `guanlan:lz:shadow:<code>`):进场=买入条件单触发按**信号价**记 open 持仓、出场=止损/止盈到价平、`shadowMetrics` 已平复利+未平现价 MTM 浮动。`luozi-app` 接 onTrigger 进场 + quote 轮询查出场 + 「开始跟踪」设上线日(today ISO)+ 算 `shadowM`;`OrderWatchPanel` 两处 onTrigger 带上 stop/take。`MetricsStrip` 实盘分支三态:**未跟踪→空态「实盘绩效未开始跟踪(不显历史回测数字)」+「▶ 开始跟踪」**、已跟踪→影子6卡(累计·影子/已实现/浮动/持仓/胜率/盈亏比)+「信号绩效·影子组合·自 YYYY-MM-DD 起·未计真实成交」诚实标;**复盘分支逐字不动**。**验真(9998/9999 同码)**:纯函数 止盈110→realized0.1/winRate1、guard(无goLive/观望/重复id)全对;实盘空态→点开始跟踪→影子卡 自2026-06-08 持仓0;**reload 后仍显影子卡(localStorage 持久)**、实盘无回测数字;复盘 6 卡照旧;0 error。**注(本期不做,归后续)**:maxHold 出场、卖信号出场、**持仓时研判变继续持/平**、跨票聚合、B2 回填真实成交;进出场真链路(买入触发→平仓)逻辑经纯函数验,真盘未现场触发(信号价未到)。
  - **遗留(5min)**:5min `量比` 用"当根/近10均量",当根为盘中 forming bar 时量能偏小(volRatio 可能瞬时很低)→ 放量类条件偏保守;可改用上一根已完成 bar 或全日均量,待优化。
  - **遗留**:③ 周期 parity——`maDiff20/rsi/量比` 回测按 5min、live 端 `live_eval` 按日线/腾讯;**但生成(/seats/order)与 live 监控(/seats/live_eval)同口径** → live 全程自洽;仅"回测 5min 检验"与 live 口径略不同(价格触发跨周期无歧义)。用户暂定不加 timeframe。
- **盯盘系统剩余**:更多 FM/combo 历史日(`fm_backfill.py`,~12min/日);LGB/v4 历史(重构 `v4_ranking.main()→rank_universe(date)`);历史 regime 归档/重算;④-③ 实时价喂 scanSeat 出盘中预警(用户暂缓,需防未完成 bar 伪造决策);⑤ 进阶:回放中自动批量真 agent(慢,需排队/缓存,默认仍 on-demand)。
- **④ 实盘真盯盘**:`/quotes` 实时价 + `/watch/bars` 5min + signal_pack + market_status 拼今日盯盘。
- **⑤ 席位真决策**:`scanSeat` 价量启发式 → 综合 卡+因子+研报 的真 agent。
- 后端落点遵 memory「后端落点在仓内」(`guanlan_v2/seats/`);改后端需重启服务生效。

## 开放项 / 接口契约(没有的接口先空着,不伪造)
> 下列是落子各证据层**该接什么形状**,供引擎 / 其它板块对齐;就绪后逐层替换 `evidenceFor()` 的 mock。当前**K 线(日线 + 历史 5min)+ 可变时间尺度接真**,其余证据层(因子/研报/regime)按用户指令暂留 mock。

1. **量化因子证据**(决策卡「触发·量化因子」:combo / FM簇 / FM分位 / LGB排名 / v4)
   - 源:引擎 `GET /watch/signal_pack?date=&top_n=` → rows `{code,date,fm_cluster,fm_pct,combo_pct,lgb_rank,lgb_pct,v4_rating,v4_score,f10_*,mainline_state,report_summary}`。
   - 阻塞:① 引擎**无「code+历史日期→因子向量」的 PIT 端点**,`signal_pack` 的 `date` 仅筛预存行 → **复盘历史因子取不到**,只「今日」可真;② 多数字段(lgb/v4/f10/主线/研报摘要)据引擎注释**分阶段填充、当前多为空**。→ 复盘因子留 mock;实盘·今日可先接。
2. **市场状态**(市况条 / 决策卡 regime·主线·涨停)
   - 源:引擎 `GET /watch/market_status` → `{regime{state,confidence},limit_ups{total,up_5d},mainline{top:[{name,lead_stock,strength}]}}`;仅「今日」,无历史 regime。→ 实盘可接,复盘留 mock。
3. **研报 / 情绪**(决策卡「引用·研报观点」)
   - 源:引擎 `GET /comments?code=&sentiment=1`(雪球评论 + bull/bear/neutral 情绪,按 code,365 天**有历史**)。可接;需对齐到「§ 脚注」式展示。
4. **席位真决策(agent 综合三方)**:当前落子触发仍是 `scanSeat` 的价量启发式,**非**「综合 经验卡 + 因子 + 研报」的真 agent 判断。待 1–3 就绪后实现;`evidenceFor()` 是其占位。
5. **实盘·今日真证据链**(原计划 Slice 1):把 1–3 接进「实盘」模式(`/watch/bars` 5min 已真;+ `signal_pack` + `market_status` + `comments` + `/quotes`)做今日单标真证据链。
6. **数据质量(stock_data 侧,落子不修)**:`vol` 单位疑似跨时间不一致(2025 段≈股、2026 段≈手),部分近端 `amount` 异常;前端只用相对量,跨界窗口或有 100× 量能突变 → 属 stock_data,登记不修。
7. **日内 TF —— ✅ 已接真 5min**(见状态 Slice 1.5):stock_data 完整历史 5min(`get_data_paths().qlib_5min` = `cn_data_5min`,**2018-01 起**;600519 97776 根)经 `/seats/daily?freq=5min` 取,`luozi-data.jsx` `frameData` 按日分组聚合成 60/30/15(perGroup=12/6/3),`subdivideDay` 降为回退。**遗留**:① 真 5min 只懒加载最近 ~2400 根 ≈ 50 交易日,游标拖到更早的日内窗口 → 回退合成(标「日内·合成」);可改 on-demand 按窗口取。② 聚合按 48 根/日顺序切块,午休边界非严格时钟对齐(值真、形态可用)。(注:引擎 `/watch/bars` 是 pytdx 实时口、仅最近 ~240 根,**别**当历史源。)
8. **架构记录**:`/seats/daily` 落 `guanlan_v2/seats/`(随 cards 先例的 guanlan 自有路由挂薄壳),**有意覆盖** 任务开头「后端加 fa-watch-wt」硬规则——引擎已 fork 进仓内 `engine/`(默认加载),fa-watch-wt 为待删上游。`event` 真事件数据缺,事件席在真 K 上暂不触发。

**2026-06-10 · 时钟兜底修正(luozi-data.jsx `?v=20260610a`,审计 M5)**:`_normClock(c, tpl)` 缺省随策略**所属模板**走 —— 此前恒取 `momentum.clock`,反转/事件策略缺字段会被动量的 止损0.08/持有30日/hourly 污染出单;`strategySave`/`scanSeat` 传入所属模板,不传仍退 momentum(向后兼容;已存策略显式字段优先、不受影响)。浏览器内实测:`_normClock({}, reversal)` = daily/0.05/13 ✓、`_normClock({})` 动量缺省不变 ✓、零报错。

**2026-06-11 · 互通批(P0①/P1⑤/P1⑨,`luozi-data/foundry/panels ?v=20260611b`、`app ?v=20260611e`)**:
- **P0① demo 假料显形**:料库卡与配方 chip 按 `demo` 渲染「示例」徽章;`recipeForStrategy` 给 demo 物料名加「(示例)」后缀(进 decide prompt 与落盘);`seedDefaultStrategy` 默认 refs 改空 + `_stripDeadDemoRecipe` 一次性迁移(仅未改名「动量 · 默认」剥 card_north/fa_north 北向死数据对,用户自加物料不动)。验真:迁移后默认策略只剩用户自加 refs、料库 8 徽章在位。
- **P1⑤ 研判喂研报正文**:recipe research 带 `path`;decide 请求体 research 改对象(panels 两处);后端读 out/ md 抽「一、综合评级/八、操作建议」(≤2篇×700字)进 prompt,落盘补 `research`/`research_excerpt_n`。验真:立昂微研判 rationale 直引研报「综合评级4/10」,落盘 excerpt_n=1。
- **P1⑨ 接收选股出口**:mount 延时 600ms `GL.take('cockpit')` —— 篮子池内票 `setCode` 聚焦 + **常驻通知条**(直插 DOM、点 × 关;React toast 状态置上但提交不显形 + timed 版被时间窗吞,故原生节点常驻),池外票诚实提示「固定 6 只,扩池另行规划」;图谱 focusSeat/focusId 兜底提示;顺修 toast「看图谱」死链(`../graph/`)。验真:真点选股「据此落子」→ GL decision + 交棒 → 落子聚焦贵州茅台 + 通知条截图在案。
- ~~遗留探讨:盯盘池扩池~~ → **已完成,见下方 2026-06-11 扩池条目**。

**2026-06-11 · P2-E 料库因子显 IC(`luozi-data.jsx?v=20260611c`)**:factorlib store 现下发验证快照 RankIC(meta.ic.rank_ic_mean,P2-E),syncArchive 透传 `f.ic`;同步守卫由「已存在即跳过」改「expr/ic 无变化才跳过」——否则本机已入 GL 的 39 个因子(ic:'')永远收不到新 IC。base 库无快照 → ic 空,诚实缺席。

**2026-06-11 · 盯盘池扩池(`luozi-data.jsx?v=20260611d` + `luozi-app.jsx?v=20260611f`)**:
- **动态票池**:`lzPoolAdd/lzPoolRemove/lzPoolIsDynamic`(luozi-data)——SYMBOL_META/SYMBOLS 原地增删 + localStorage `guanlan:lz:pool:v1` 持久;固定 6 只为底座不可移,动态票带 `dynamic:true`。合成兜底种子由代码哈希派生(仅 file:// 离线显形,带既有「样例」标);**有后端时真日K 由既有 per-code 拉取自动接管**(对任意 code 通用)。
- **选股篮子整篮入池**:cockpit 交棒分支改为逐票 `lzPoolAdd`(幂等)+ 聚焦首票 + 常驻通知条报「新入盯盘池 N 只」;`setPoolTick` 补一拍重渲染(池数组是模块级)。
- **顶栏**:下拉动态票标「·池」;当前为动态票时显「移出盯盘池 ×」pill(移出后回落 LZ_PRIMARY)。
- 验真:篮子 2 只(坤恒顺维/立昂微)入池 → 聚焦坤恒顺维**真·日线**(真价 35.86 · 250 根 · 决策流水真算)→ 普通刷新池仍 8 只(持久)→ 真点「移出」立昂微 → 池 7 只、localStorage 单条、视图回落宁德。坤恒顺维留池作示例,可一键移出。

**2026-06-14 · 回测可信卡体系 P1(逐日 PIT 上下文 + 删假料,`luozi-data ?v=20260614a`、`luozi-panels/app ?v=20260614b`;后端 `guanlan_v2/seats/narrative.py` 新建 + `seats/api.py` decide 接线)**:
- **痛点**:此前回测 agent 每决策步喂的「研报/regime」是**死料**——50 笔全程同一份固定 research(前端 `runRealThink`/`runTimedDecide` 透传配方里的 research)、`regime=null` 无大盘、`distillToCard` 伪造 IC(`0.02+sharpe*0.008`)。审计判定「不是真能用的」。
- **叙事卡按日浮出(替代固定透传)**:新建 `narrative.py` 纯函数 `build_pool`/`surface_narratives`/`regime_asof`(TDD)。decide 不再吃前端 research,改后端 `_surface_for_decide(code, industry, asof)`:以决策日 `asof` 为 PIT 锚,从 `out/` 研报(文件名 `<CODE>_<YYYY-MM-DD>.md`)+ `var/archive/` 叙事卡里取 `as_of≤asof`、关联本票/行业、近 N 天(研报窗 60 / 新闻 10 / 复盘 30)、topK=6 浮出,**只喂 LLM**。游标前进新料冒出老料沉底 = 每日不同研报;无相关料 → **诚实空**(绝不复用、不注入示例)。前端 `runRealThink` + 实盘 `runTimedDecide` 均删死 research、改传 `industry`。
- **大盘日产物(修 regime=null)**:`regime_asof(date, breadth_df)` 读平台逐日 breadth 产物(`MARKET_BREADTH_PARQUET`,datetime 索引),按 `idx≤date` 切当日大盘点评(`lu_resid`/`amt_resid`/`pct60`)。日线用决策日 EOD;30min 盘中用**上一交易日** EOD(当日 EOD 那时尚不存在,喂它=看未来)。落盘补 `regime_asof`/`regime_asof_used`/`narratives_surfaced`。`RunDecCard` 新增「当日浮出研报(PIT 按日)」+「当日大盘(PIT 日产物)」两字段(读 `dec.research`/`dec.regime_asof`,经 `/seats/decisions` 完整记录回传 → runDecs 透传)。
- **删假料**:`distillToCard` 删伪 IC、insight 改「【复盘草稿·未经验证,不入信号】」+ `status:'draft'`;`recipeForStrategy` 对 `demo` 物料 `if (a.demo) return;` **从真路径剔除**(不进配方、不喂 decide)。
- **PIT 红线**:vintage + as_of 闸门 + 面板/日产物全 as-of D,零看未来;严格滚动。
- **验真(真数据 live)**:立昂微(SH605358)两决策日 diff —— **D1=2026-06-10**:浮出 3 条真研报(06-10/06-09/05-23,皆 ≤ 决策日)、`regime lu_resid=113.850`(截至 06-09);**D2=2026-03-01(PIT→02-27)**:研报 **0 条·诚实空**(那些研报都在未来,PIT 正确排除)、`regime lu_resid=-6.539`(截至 02-27)。同票逐日叙事/大盘真变化、无相关料即空,核心主张成立。pytest **262 绿**;浏览器 `?v=20260614b` 0 console error、两新字段就位、实盘路径改 industry、伪 IC/死料净空。
- spec `docs/superpowers/specs/2026-06-14-backtest-trustworthy-cards-design.md` / 计划 `docs/superpowers/plans/2026-06-14-p1-daily-pit-context.md`。**挂账 P2**:量化卡 vintage IC;**P3**:加权混合进信号(面板求值器);历史新闻回填(akshare 停 03-30 后回测日叙事主要靠研报+复盘)。

**2026-06-14 · 回测可信卡体系 P2(量化卡 vintage IC,`luozi-data/app ?v=20260614c`、`luozi-panels ?v=20260614d`;后端 `guanlan_v2/screen/factor_vintage.py` 新建 + `seats/api.py` decide 接线 + `regen.py` step 3.6)**:
- **问题**:因子卡的 `ic` 是 `compute_catalog_ic` 算的**静态 60 日截面 rank-IC,算到 regen 最新日**(`factor_ic.parquet asof=end`)。回测某天 D 决策时 decide 给 agent 看的 `IC=...` 用了 D 之后的数据 = **IC 数值本身看未来**。
- **逐日 vintage IC(两口径)**:新 `factor_vintage.py` 一次面板加载 + 一遍因子编译同产两表 —— **截面**(全 catalog 442+56)`var/factor_vintage_cs_ic.parquet`[id,date,ic,n,realized_date](逐日截面 rank-IC 全留不 mean)、**单票 tsic**(`SEATS_POOL_CODES` 固定盘 7 票 × factorlib 因子)`var/factor_vintage_tsic.parquet`[code,id,date,fval,fwd,realized_date]。每行带 `realized_date = date+horizon 交易日`(fwd 实现日)。`compute_factor_vintage` 挂 regen step 3.6(失败不阻断三产物)。
- **PIT 真 OOS 闸门(命门)**:`cs_vintage_asof(id,D)`/`tsic_vintage_asof(code,id,D)` 只取 `realized_date≤D` 的 trailing window 行求 IC(cs=均值、tsic=Spearman);`<min_n=10` → `None`(诚实「样本不足」)。**绝不取 realized_date>D**。`_rf_vintage_line` 的 PIT 锚:日线=决策日、**30min 盘中=上一交易日**(当日 EOD 那时未结算,realized_date=当日 的行=看未来 → 回退排除,同 P1 regime_asof 口径;**回退按引擎全量交易日历 `_prev_trading_day`**,跳周末/假日 —— 详见下方 2026-06-14 收口项)。
- **decide 接线**:rf_line 每个配方因子先 resolve 成 catalog id(显式 id > expr > short,`_factor_id_index` 反查索引按 `len(FACTOR_DEFS)` 探针重建防 refresh 后陈旧),再**优先本票 tsic 退截面 cs** vintage as-of,`名(IC@asof=x·OOS·n·本票/截面)` 入 prompt(标「不进信号」),落盘 `recipe_factors_vintage:[{name,id,ic,n,kind,asof}]`。前端 `recipeForStrategy` 透传因子 `id`;`RunDecCard` 新增「配方因子 vintage IC(as-of·真OOS·不进信号)」字段(ic==null→样本不足灰显),旧「配方因子(未回测)」字段降级为无 vintage 时才显。
- **验真(真数据 live)**:批算产 **cs 45436 行 / tsic 170009 行**。因子 c_021b00 三决策日 vintage as-of:03-02=**+0.0287**(asof 02-13)/ 05-08=**−0.0337**(04-28)/ 06-09=**−0.0561**(05-26)——逐日真变含符号翻转、每个 asof≤决策日。立昂微 live decide 两日落盘 `recipe_factors_vintage` D1=03-02 IC 0.0287 / D2=06-09 IC −0.0561(asof 02-13 / 05-26,PIT 铁证)。pytest **277 绿**(含 cs/tsic/realized_map/decide-resolve/PIT 锚/缓存重建 共 14+ 新测);浏览器 `?v=20260614d` 0 console error。代码评审 python-reviewer + react-reviewer 均 Approved,IMPORTANT(30min asof 看未来)+ MEDIUM(catalog 缓存陈旧)已修。
- spec `docs/superpowers/specs/2026-06-14-backtest-trustworthy-cards-design.md`(P2 详细段)/ 计划 `docs/superpowers/plans/2026-06-14-p2-vintage-ic.md`。**挂账 P2b**:料库 factorlib 列表显最新 vintage IC(需新 `/factor/vintage_latest` 端点);批算 O(n²) 逐日扫描 + tsic `iterrows()` 性能(当前规模 3-8min 可接受,同 factor_ic 范式)。**P3**:加权混合进信号 `(1-w)·LLM+w·因子z`(per-step 面板求值器);历史新闻回填。

**2026-06-14 · 回测可信卡体系 P3(加权混合进信号,`luozi-data/app/panels/foundry ?v=20260614e`;后端 `factor_vintage.py` 加 `factor_z_*` + `seats/api.py` 评分函数 + decide 混合)**:
- **唯一改交易信号的一期**(P1/P2 只喂上下文/显示)。`决策偏向 bias=(1-w)·LLM分+w·因子z分`,w 每策略可调,**w=0 严格等于现状纯 LLM**。
- **因子 z 分(PIT·复用 P2 不建新求值器)**:`factor_vintage.py:factor_z_asof(code,id,asof)` 直接读 P2 的 tsic parquet 的 `fval`(pool 票×factorlib 因子逐日因子值,PIT 安全:fval 当日即知,只 `date≤asof`、无需 realized_date 闸门),`z=(当前fval−trailing均值)/std`,`<min_n` 或 std=0→None。
- **评分 + 混合(`seats/api.py` 纯函数)**:`_llm_score=sgn(direction)·conf/100∈[-1,1]`(买+/卖−/观望0);`_combine_factor_score`=每因子 `clip(dir·z,-1,1)`(dir 来自 vintage IC 符号,P2)**等权平均**,全无→None;`_hybrid_direction`:**w≤0 或 factor_score=None → 透传 LLM 方向(不经死区)** 保 w=0=纯LLM,否则 `bias=(1-w)·llm+w·factor`、`bias>τ 买/<−τ 卖/否则观望`(τ=0.15 死区)。decide 读 `payload.w`、算 5 字段(`w/llm_score/factor_score/hybrid_bias/hybrid_direction`)入响应+落盘,`_rf_vintage_line` 每因子补 `z/dir/score`。
- **前端**:Foundry 加「因子权重 w」滑块(0~1,默认 0);`strategySave` 持久化 w;`runRealThink` payload 传 w;`runDecs` 透传 hybrid;`runBacktest(runDecs,bars,useHybrid)` 跑两遍 → **双线净值**(纯 LLM 实线 + 混合朱砂虚线,经 `EquityChart` 多线)+ 归因 Δtotal;`RunDecCard` 加「混合决策(方向·bias·因子分·w)」,w=0 显「纯 LLM」。w=0 两线重合(只画单线 + 注「未混入因子」)。
- **范围/诚实降级**:回测、限 `SEATS_POOL_CODES×factorlib`(tsic 覆盖);非 pool/非库/样本不足→factor_score=None→退纯 LLM。
- **验真(真数据 live·因子真进信号铁证)**:立昂微 SH605358 因子 `lib_amount_share_5d_60d`,**同票同日(2026-03-02)同因子、LLM 都「观望」**:w=0 → 混合=观望(factor_score=0.9167 算出但不进,bias=0);**w=0.6 → 混合=买入**(bias=0.4·0+0.6·0.9167=0.55>τ,因子看多 z 经 vintage IC 定向、按权混入,**翻转了决策**)。pytest **288 绿**(factor_z/评分/hybrid/decide-resolve 共 17+ 新测);浏览器 `?v=20260614e` 0 console error、双线/滑块/混合字段字节级确认。python+react reviewer Approved(Critical/High 无,挂账质量项)。
- spec(P3 详细段)/ 计划 `docs/superpowers/plans/2026-06-14-p3-weighted-signal.md`。**回测可信卡体系 P1+P2+P3 三期全收口**:卡片从「假料」→「逐日 PIT 真重算·零看未来·可加权进信号」。**挂账**:实盘逐步实时面板求值器 + 全市场/全 catalog tsic(本期限 pool×factorlib);w 自动寻优;P2b 料库显 vintage 端点。

**2026-06-14 · 验证灰项收口 ×2(P1+P2+P3 验证 agent 浮出的两个非阻断项)**:
- **① 30min PIT 回退改按引擎全量交易日历**(`seats/api.py`):此前 `_prev_trading_day` 用 breadth 日产物索引(`breadth.index`)做交易日历,但 breadth **会滞后于真实交易日、漏算近端日**(实测 06-11 盘中回退会落到 06-09 而非 06-10)。新增 `_trading_calendar()` = 引擎 `get_default_loader()._load_calendar("day")`(**8797 日,1990-12-19→2026-12-31**,一次性缓存);`_prev_trading_day(date_str, idx=None)` 日历优先级 = 显式 idx > 引擎全量日历 > 日历−1 天兜底(保守只更早、不泄漏)。`_rf_vintage_line` 的 30min 分支 + regime 接线都改调 `_prev_trading_day(asof[:10])`(去掉 breadth.index 传参)。**live 铁证**:`_prev_trading_day('2026-06-11 10:30')→2026-06-10`、`'2026-06-08'(周一)→2026-06-05`(跳周末)、`'2026-06-09'→2026-06-08`。
- **② validation.jsx synthVal 改诚实占位**(`ui/cards/validation.jsx`,html `?v=20260614a`):经验验证区 demo/草稿卡的合成指标 `synthVal` 此前用伪 IC 公式 `ic:(0.02+conf/3000).toFixed(3)` 凭置信度造假数 —— 看起来像真 IC 实为捏造。改为 `ic/icir/ann/win/mono/n` 全 `'—'`(诚实「未验证」),只保留 `conf`/`verdict`/`demo` 真字段。浏览器 `?v=20260614a` 0 真 console error(仅 favicon 404 + babel 警告),served jsx 确认含 `'—'`、无伪 IC 公式。
- **验真**:pytest **290 绿**(新增 `test_prev_trading_day_skips_weekend_via_calendar` 显式日历跳周末 + `test_prev_trading_day_fallback_calendar_minus1` monkeypatch `_trading_calendar`→None 兜底 + 已有 `test_rf_vintage_intraday_uses_prev_trading_day` 现取引擎日历 06-10);重启 9999 拉新代码,引擎日历在真 import 路径加载成功。

**2026-06-14 · 落子真假料审计 + 6 条假料收口(`luozi-data/panels/app ?v=20260614f`)**:
- **审计**(8 路并行 finder + 逐条对抗复核,23 agent):**后端 100% 真接入零假料**——decide 真调 deepseek LLM、证据(vintage IC/叙事/regime)全 PIT 真算、失败 `ok:False` 不落盘;runs/ledger/decisions 真 JSONL 事件流、benchmark 真 etf_index 399300.SZ、calibration 真命中率、factors 走 compute_factors PIT;vintage/fm_backfill 全真算诚实 None。**前端核心链路**(真 LLM 研判 / 真 run 回测 / 真 K 线 / 真基准 / 真台账)也真。**残留 6 条假料全在前端「决策卡证据外壳」,同一根因** = `evidenceFor()` 合成料(combo/FM/LGB/v4 拍脑袋公式、写死研报句、模板卡、按相位 regime)挂在每条 scanSeat 启发式决策上,经两条 live 可达路径(复盘推演动画、实盘信号队列→决策卡)在真值缺位时**无徽章漏出 + 把启发式说成 agent 思考**;为 backtest 写的诚实小字被 `mode==='backtest'` 门控、没覆盖 live。
- **6 条收口**(纯前端、不动真链路):① 复盘推演浮层 `Deliberation` 文案「正在推演·研读经验卡/研报/量化因子」→「启发式扫描 · MA/量比价量规则(scanSeat·非 LLM)· 真 agent 研判用『✦ 让 agent 真跑』」,控制条「agent 思考中…」→「启发式扫描中…」(luozi-app.jsx);② DecisionCard 顶加 `isHeuristic` 横幅「启发式骨架 · 非 LLM —— 方向/置信/因子均 scanSeat 估算」(luozi-panels.jsx);③ 实盘「信号队列」表头加「启发式 · 非LLM」徽章 + 每行 `d.ev.card.name`(合成卡名)→ `d.note`(真启发式规则文案);④ 触发因子 else 分支不再展示 `ev.factors` 合成值、一律「—」,诚实小字门从 `mode==='backtest'&&!signal` 放宽到 `!signal`(覆盖 live 无 signal);⑤ 市场状态 live 缺真值不回退合成 `ev.regime/ev.mainline`(显「—」+诚实小字),主线「真·今日」徽章与真实主线绑定;⑥ 经验卡无真卡(`!sc`)回退 `ev.card` 时加「示意 · 未引用真卡」徽章 + 诚实小字;并在 `evidenceFor` 上钉合约注释(消费方无真值必降级/标徽章)。
- **验真(浏览器真跑 ?v=20260614f)**:JSX 0 解析错(仅 favicon 404 + babel 警告);served 三文件含全部新诚实串、旧误导串(「agent 思考中…」「研读经验卡/研报/量化因子」)已消失;实盘模式「信号队列」徽章渲染 + 行显真规则文案(「MA5 上穿 MA20,量价齐升…」非合成卡名);点开买入信号 → DecisionCard 渲染「启发式骨架·非 LLM」横幅 + 「示意·未引用真卡」徽章;宁德 300750 有真 signal_pack/market_status → 走真路径(combo「真·今日」+真 regime),诚实 fallback 不误触发(符合设计:仅 signal 为 null 才显)。**后端无改动,9999 不重启**。

**2026-06-14 · 校场料库加「编辑」删卡(`luozi-foundry.jsx ?v=20260614g`)**:校场右栏「料库」(共享档案库,卡/研报/因子三 tab)头部加「编辑/完成」toggle(`matEdit`)。编辑态:每张物料卡右侧 ＋/✓ 换成「删」按钮,**两步防误删**(点「删」→ armed 显「确认删」红 + 「×」取消;`delArm` state,切 tab/退出编辑自动清),确认调既有 `GL.remove(id)`(从共享档案库移除 + 持久化 localStorage + 后端 `/archive/remove` fire-and-forget,**各页同步**),若该卡正被当前策略引用则顺手 `delRef` 清配方。编辑态禁拖拽/禁 ＋ 入配方;头部红字提示「点卡片「删」从共享档案库移除(各页同步 · 不可撤销)」。纯前端复用既有 GL primitive,后端零改动。**验真**:浏览器真跑 ?v=20260614g 0 解析错;校场编辑态 20 张卡各显「删」、点「删」→「确认删」armed、点「×」取消后卡数仍 20(非破坏验证,未删真卡);删除路径=既有 `GL.remove`(验证区/图谱同源,已久经使用)。

**2026-06-14 · 料库加回收站(软删除,`luozi-foundry.jsx ?v=20260614h`)**:把删卡从「直接 `GL.remove` 永久删」改成**软删除 → 回收站可恢复**。料库头部加「回收站 N」toggle(`trashOpen`);删卡时 `trashItem(it)` 先把完整 artifact(`GL.get`)存进 localStorage `guanlan:luozi:trash:v1`(`{id→artifact+_trashedAt}`)再 `GL.remove`(顺手 `delRef` 清当前策略配方);回收站视图列出已删物料(标题+类型徽章+示例徽章),每条「恢复」=`GL.put(art)` 放回共享档案库(research 类自动回推后端 `/archive/put`)、「彻底删」=从 trash 抹除,顶部「清空回收站」。编辑态小字改「点卡片「删」移入回收站(可恢复;不彻底删)」。**注**:回收站是**客户端(本浏览器 localStorage)**的撤销缓冲,非后端;research 类软删时已 `/archive/remove`(防 `/archive/list` 合并复活),恢复时 `/archive/put` 回补,口径自洽。**验真(浏览器真跑 ?v=20260614h,完整 round-trip 净零)**:0 解析错;删「缩量企稳反转」→ 卡 20→19、trash=1、不在档案库;开回收站显该卡+恢复/彻底删/清空;点「恢复」→ 卡 19→20、回到档案库、trash=0(用户档案库恢复原样,非破坏验证)。纯前端、未动 `guanlan-bus.js`、后端零改动、9999 不重启。
